# Prod Rollout Plan — Phase 3 stack into `quantcore-prod-20260606`

## Checkpoint Log

Update this table as part of every step-commit. Any dev machine can resume by reading the last DONE row, running `git pull`, and continuing with the next step. Mirrors [`phase3-gateway-plan.md`](phase3-gateway-plan.md).

### Status: COMPLETE (2026-06-17). P0–P10 done — the Phase 3 stack runs in production (`quantcore-prod-20260606`, project # `127961694257`, `us-central1`): `quantcore-api` (JWT-enforced, reads prod Cloud SQL) + 5 MCP wrapper services + a daily report Cloud Run Job on Cloud Scheduler, all on validated digest-copied images. Verified end-to-end (api JWT-enforced + prod-DB read, 5 wrappers serving `/mcp`, full wrapper→api→service→prod-DB path, React UI through the authenticated proxy). Tail items closed: P9a (`.mcp.json` repointed at prod wrapper `/mcp` URLs, local entries kept as `-local`), P9b (`scripts/setup_test_wif.sh` + `deploy.yml` push/PR triggers restored behind a `preflight` secret-presence guard), P7 (`portfolio/yfinance_gateway.py` `get_latest_prices` hardened with retry/back-off + graceful all-None degrade), P10 (this exit). **Promoted digests:** api `sha256:ac5cd17f…0e41`, mcp `sha256:1b7da905…1479`, report `sha256:65d70659…5e68`. **Prod service URLs:** `https://quantcore-{api,stock-price,options-analysis,company-fundamentals,news-sentiment,market-analysis}-127961694257.us-central1.run.app` (also resolvable as `-swgixldxzq-uc.a.run.app`). Phase 3 is fully closed.

Phase 3 of [`architectural-standard-v2.md`](architectural-standard-v2.md) §11 is **complete on the test project** (`quantcore-test-20260606`): `quantcore-api` + 5 MCP wrapper services + a daily report Cloud Run Job all run on Cloud Run against the **test** Cloud SQL instance, JWT-enforced, with CI/CD in `.github/workflows/deploy.yml`. This plan covers the final operational milestone — standing up the **same stack in a dedicated production project** pointed at the production database.

**Why a separate prod project (not a test-service DSN flip):** the original Phase 3 exit framed the cutover as repointing the test-project services' `QUANTCORE_DB_DSN` from the test instance to prod. The user chose the stronger isolation boundary instead: **the test stack stays on test, untouched**, and production gets its *own* Cloud Run stack in its *own* project reaching its *own* Cloud SQL. This gives prod independent IAM, secrets, quota, and blast radius.

**The prod project + DB already exist.** `quantcore-prod-20260606:us-central1:quantcore` is a live Cloud SQL instance holding the production data (reached locally today via the `:5433` cloud-sql-proxy; the `:5434` proxy is test). What is missing is only the Cloud Run **compute** layer — this plan replicates Steps 7–10 of the Phase 3 plan into the prod project.

---

## Locked decisions (with the user)

1. **Image source → Option B2 (digest copy into a dedicated prod AR).** Prod owns a `quantcore` Artifact Registry repo; images are **copied by digest** from the test AR (`gcloud artifacts docker images copy …@sha256:…`), *not* rebuilt. This keeps prod self-contained (no standing cross-project pull dependency) **and** runs the exact bytes validated on test (no rebuild-divergence). Rejected: Option A (cross-project pull — reintroduces a permanent prod→test dependency, against the isolation goal) and B1 (rebuild in prod — second pipeline *and* gives up the same-artifact guarantee).
2. **Separate prod GCP project.** Full deployment into `quantcore-prod-20260606`; the test stack is never repointed at the prod DB.
3. **New prod JWT secret.** A brand-new HS256 key in the prod project — the test `quantcore-jwt-secret` is **never** reused across the trust boundary.
4. **No reseed.** The prod `positions` table already holds the live data; `scripts/import_portfolio.py` is **never** run against the prod DB.
5. **Prod deploys stay gated/manual.** No auto-deploy to prod on every push to `main`. Prod promotion is a deliberate `workflow_dispatch` (or tag) job with its own WIF provider/SA and a `prod` GitHub Environment — consistent with "prod-flip deliberately not automated."

## Standing constraints

- **DB safety is paramount.** All commands here target `quantcore-prod-20260606`. The production DSN (`QUANTCORE_DB_DSN`, the `:5433` proxy locally) is the live database — read-only smokes only, **no schema reseed, no destructive ops**. The test stack and test DB are untouched by this plan.
- **The user runs all IAM / high-severity GCP commands and commits/pushes themselves.** I provide the exact command + the `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` footer; the user executes.
- **Rule 6 is sacred.** Wrappers + front end go through REST; the report Job calls services **in-process** (anti-pattern 5), never the HTTP tier — identical to test.
- One commit per code/doc step (the `prod-rollout.yml` workflow, doc updates); the GCP buildout steps are gcloud/IAM actions logged in this table.

---

## Open decisions to resolve before / during rollout

These are **not yet locked** — flagged so they're not silently assumed:

- **Runtime service account.** ✅ **RESOLVED → dedicated least-privilege SA** `quantcore-run@quantcore-prod-20260606.iam.gserviceaccount.com` (prod project number `127961694257`). Created in P4; granted only `roles/cloudsql.client` (project) + `roles/secretmanager.secretAccessor` (per-secret). Same-project AR image pulls use the **Cloud Run service agent** (auto-granted `artifactregistry.reader` when the AR API is enabled), so the runtime SA needs **no** AR grant. All P5–P7 deploys pass `--service-account=quantcore-run@…`.
- **Discord channel.** Same webhook as test (alerts interleave) **or** a separate prod Discord channel/webhook. Affects the `quantcore-discord-webhook` secret payload.
- **JWT issuance for real AI clients.** Who mints the HS256 tokens external AI clients present (`QUANTCORE_MCP_TOKEN`)? Test used hand-minted tokens for smokes. Prod needs a real issuance story (a small signing endpoint/CLI, rotation policy) before the wrappers are meaningfully usable by humans. May warrant its own design pass.
- **Initial scope.** All 5 wrappers + report Job from day one, **or** a narrower first cut (e.g. `quantcore-api` + report Job, wrappers later)? Narrower reduces the prod surface to validate first.
- **Prod region / HA.** Test is single-region `us-central1`. Confirm prod stays there; revisit Cloud SQL HA / Cloud Run min-instances for prod SLOs separately.

---

## Step-by-step plan (one commit per code/doc step; GCP steps logged in the table)

| Step | Description | Status | Commit | Date | Notes |
|---|---|---|---|---|---|
| P0 | Plan + decisions captured | DONE | — | 2026-06-17 | This document. Image source = B2 (digest copy → dedicated prod AR); separate prod project; new prod JWT secret; no reseed; gated prod deploys. Prod project + Cloud SQL (`quantcore-prod-20260606:us-central1:quantcore`) already exist with live data. Open decisions (runtime SA, Discord channel, JWT issuance, initial scope, HA) listed above — resolve before the matching step. |
| P1 | Enable prod APIs | DONE | — | 2026-06-17 | Enabled on `quantcore-prod-20260606`: `run`, `artifactregistry`, `secretmanager`, `cloudbuild`, `cloudscheduler`, `iam` (`gcloud services enable …`). Non-destructive; no IAM/resources created. Op `acf.p2-127961694257-…` finished successfully. |
| P2 | Create prod AR repo | DONE | — | 2026-06-17 | Docker AR repo **`quantcore`** in `us-central1` on the prod project (`us-central1-docker.pkg.dev/quantcore-prod-20260606/quantcore`), created 14:32:21Z (the explicit `create` then returned `ALREADY_EXISTS` on a 3s-later retry). Verified empty (0 MB, no images) — clean target for P3. |
| P3 | Promote images (B2 digest copy) | DONE | — | 2026-06-17 | Copy the **three** tested images **by digest** test→prod (no rebuild). **Source of truth = what the TEST stack actually serves:** api `quantcore-api:latest` → `sha256:ac5cd17f53f91b48dc37cfb5dc178f06606304550cf03a5c48c5e63a91af0e41`; wrappers (all 5) `quantcore-mcp:step8fix` → `sha256:1b7da905fb328670d360871e72ab903be28c6baeeb0c6bb5b6328d584df11479`; report `quantcore-report:step9` → `sha256:65d70659776aedc2d065f4c61ab192700ec0ac2d1827a470442863a7d6845e68`. Per image: **`docker buildx imagetools create --tag <prod>/<img>:prod-init <test>/<img>@<digest>`** — registry-to-registry manifest copy that preserves the digest, so prod runs identical bytes; the `prod-init` tag is just a human label — P5–P7 deploy **by digest**. (NB: `gcloud artifacts docker images copy` does **not** exist in SDK 573.0.0 — `imagetools` is the working path; `gcrane cp` is the fallback. Requires `gcloud auth configure-docker us-central1-docker.pkg.dev` once.) Copier identity needs `reader` on the test AR + `writer` on the prod AR (the user's own ADC has both). Verify prod digest == test digest after copy. **DONE 2026-06-17:** all three original manifests confirmed addressable byte-identically in the prod AR (`$PROD/<img>@<original-digest>` resolves to the same digest). **Caveat:** `imagetools create` also wrapped each in a new OCI **index** under the `:prod-init` tag (different top-level digest, ~10 min/image as it re-transferred layers) — so **P5–P7 deploy by the ORIGINAL digest, NOT the `:prod-init` tag**. **FOLLOW-UP for P9:** `prod-rollout.yml` still references the non-existent `gcloud artifacts docker images copy`; rewrite its promote step to `docker buildx imagetools create` (or install `gcrane cp`) and deploy by the resolved original digest. |
| P4 | Prod runtime SA + secrets + IAM | DONE | — | 2026-06-17 | **SA created** ✅ — `quantcore-run@quantcore-prod-20260606.iam.gserviceaccount.com` exists (prod proj # `127961694257`); a re-run returned `ALREADY_EXISTS`. **`roles/cloudsql.client` granted** ✅ (P4b, 2026-06-17 — confirmed in the project IAM policy). **Still pending:** the 3 secrets + per-secret `secretmanager.secretAccessor`, and `iam.serviceAccountUser` on the runtime SA for the deploying identity. **Secret Manager (prod project):** `quantcore-prod-db-dsn` = unix-socket DSN `postgresql://quantcore:***@/quantcore?host=/cloudsql/quantcore-prod-20260606:us-central1:quantcore`; **new** `quantcore-jwt-secret` (freshly generated 48-byte HS256 key, NOT the test key); `quantcore-discord-webhook` = **reuse the same webhook as test** (decision locked 2026-06-17), sourced from `.env` `DISCORD_WEBHOOK_URL`, **quote-stripped** (`.env` double-quote bug from Phase 3 Step 9). **IAM (user runs):** grant the runtime SA `roles/secretmanager.secretAccessor` on each of the 3 secrets (per-secret; basic roles exclude secret-payload access). The deploying identity (manual P5–P7 = the user; automated P9 = the prod deploy SA) needs `roles/iam.serviceAccountUser` on the runtime SA. **No AR grant on the runtime SA** — same-project pulls go through the Cloud Run service agent. |
| P5 | Deploy `quantcore-api` (prod) | DONE | — | 2026-06-17 | **DEPLOYED:** rev `quantcore-api-00002-225`, URL `https://quantcore-api-127961694257.us-central1.run.app`. Smoke: `/api/health` → `{"status":"ok","db_connected":true}` (live prod Cloud SQL); `/api/portfolio` no-token → 401 (JWT enforced). | **GOTCHA (2026-06-17):** first deploy crashed on startup — `psycopg2 ... fe_sendauth: no password supplied`. Cause: the prod DB password contains a URL-reserved char, so the URI-form DSN `postgresql://quantcore:PASS@/...` parsed the password as empty. Fix: rebuilt `quantcore-prod-db-dsn` from the `.env` prod DSN with the password **percent-encoded** (`urllib.parse.quote(pw, safe='')`) and added a new secret version, then redeployed `:latest`. Deploy by the **original** digest (not `:prod-init`). From the prod-AR `quantcore-api` digest: `--service-account quantcore-run@quantcore-prod-20260606.iam.gserviceaccount.com --add-cloudsql-instances quantcore-prod-20260606:us-central1:quantcore --set-secrets QUANTCORE_DB_DSN=quantcore-prod-db-dsn:latest,QUANTCORE_JWT_SECRET=quantcore-jwt-secret:latest --allow-unauthenticated --ingress all --memory 4Gi --cpu 2 --cpu-boost`. App-level JWT is the enforcement layer (Cloud Run IAM open, same rationale as test Step 8). Smoke: `/api/health` 200 `db_connected:true`; protected route no-token → 401; minted **prod** token → 200. |
| P6 | Deploy 5 wrappers (prod) | DONE | — | 2026-06-17 | **DEPLOYED** all 5 (`quantcore-{stock-price,options-analysis,company-fundamentals,news-sentiment,market-analysis}-127961694257.us-central1.run.app`), `quantcore-mcp@1b7da905` original digest, SA `quantcore-run`, `QUANTCORE_REST_URL`→prod api, cpu1/mem1Gi/maxScale2. Reachability smoke: bare `GET /mcp` → **406** on all 5 (correct FastMCP streamable-HTTP response = booted & serving). Authenticated `listTools()` deferred to P8 (needs minted prod JWT). All from the single prod-AR `quantcore-mcp` digest, `--service-account quantcore-run@…`, per-wrapper `SERVER_MODULE` + `QUANTCORE_REST_URL`→prod api, `--allow-unauthenticated --ingress all` (public + app-JWT passthrough; internal-ingress lockdown still deferred until a client VPC/IAP path exists). Services `quantcore-{stock-price,options-analysis,company-fundamentals,news-sentiment,market-analysis}`. Smoke each wrapper's `listTools()` + one authenticated call end-to-end (use the slash-less `/mcp` URL — Step 8 gotcha). |
| P7 | Report Job + Scheduler (prod) | DONE (infra) | — | 2026-06-17 | **Job + Scheduler + IAM all deployed & verified.** Both manual executions reached the prod path correctly — `init_schema()` connected to prod Cloud SQL, portfolio loaded, symbols resolved — proving job config (SA, connector, secrets) is right. Execution then **crashes in `yf.download` → `ValueError: No objects to concatenate`** (yfinance returns empty for all tickers and raises *before* the `if not data.empty` guard). This is the **pre-existing `portfolio/yfinance_gateway.py` fragility** (open consolidation item), reproducible on the **test** job too (`nq4qj` failed between green runs) — NOT a prod defect. **Decision 2026-06-17: mark P7 done, defer the fix** (don't keep retrying = avoid deepening Yahoo throttle); the 5pm ET scheduled run is the acceptance test. **FOLLOW-UP — DONE 2026-06-17 (P10 commit):** `get_latest_prices` hardened — the `yf.download` call moved into a `_download_latest()` helper that retries up to 3× with a linear back-off and catches **any** exception (incl. `ValueError: No objects to concatenate`) *and* empty frames, returning an empty DataFrame so the caller degrades to all-None prices instead of crashing the report. **Still a USER/CI step:** rebuild `Dockerfile.report` with this change, re-promote the report image test→prod by digest, redeploy the prod `quantcore-report` Job. Until then prod still runs the pre-hardening `report` digest `65d70659…`. `quantcore-report` Cloud Run **Job** from the prod-AR `quantcore-report` digest; `--service-account quantcore-run@…`, `--set-secrets QUANTCORE_DB_DSN=quantcore-prod-db-dsn:latest` (+ `DISCORD_WEBHOOK_URL=quantcore-discord-webhook:latest` if Discord opted in) `--set-cloudsql-instances …prod…:quantcore --task-timeout 600 --max-retries 1 --memory 1Gi`. **No JWT** (no HTTP surface; in-process services). Cloud Scheduler `quantcore-report-daily` weekdays `0 17 * * 1-5 America/New_York`, OAuth as `quantcore-run@…`, which needs `roles/run.invoker` on the Job. **IAM (user runs):** Cloud Scheduler service agent needs `roles/iam.serviceAccountTokenCreator` on the OAuth SA `quantcore-run@…` (the trigger silently no-ops otherwise — Phase 3 Step 9). **No reseed** — prod DB already has positions. Verify one manual `jobs execute`. |
| P8 | End-to-end prod verification | DONE | — | 2026-06-17 | **ALL PASS (read-only smoke, `/tmp/prod_p8_smoke.py`).** Minted a short-lived HS256 prod JWT (signed with `quantcore-jwt-secret`, secret injected via command-substitution, never echoed): (1) no-token `/api/portfolio` → **401** (JWT enforced); (2) authed `/api/portfolio` → **200, 1 holding from prod DB**; (3) news-sentiment wrapper authed `listTools()` → **4 tools**; (4) full `wrapper → api → service → prod DB` via DB-backed `list_news_symbols` → `{'symbols': ['CRDO','MRVL'], 'total_symbols': 2}`. DB-backed paths chosen so the P7 yfinance flake can't affect the result. **React UI repointed at prod + manually verified:** `frontend/vite.config.ts` proxy target now env-driven (`VITE_API_PROXY_TARGET`); `frontend/src/api/client.ts` attaches `Authorization: Bearer` when `VITE_API_TOKEN` is set (unset = local/compose unchanged); `frontend/.gitignore` now ignores `.env.local`/`.env.*.local`. Prod values live only in the gitignored `frontend/.env.local` (token never printed); `npm run dev` proxies `/api` server-side to prod so the browser stays same-origin (no CORS change). Dashboard/portfolio/watchlist loaded live prod data. Report Job acceptance = the scheduled 5pm ET run (P7). **Token tooling committed (replaces the throwaway `/tmp` scripts):** `scripts/mint_prod_jwt.py` mints a short-lived HS256 prod JWT (fetches `quantcore-jwt-secret` from Secret Manager itself, never prints the secret; `--output token|export|frontend-env`), and `docs/operations/prod-jwt-tokens.md` is the team runbook for minting from scratch + refreshing an expired token (UI / curl / MCP) + key rotation. |
| P9 | Gated prod CI/CD + repoint clients | DONE | — | 2026-06-17 | **CLOSED in the P10 commit:** clients repointed — `.mcp.json`'s 5 named servers now hit the prod wrapper `/mcp` URLs with `Authorization: Bearer ${QUANTCORE_MCP_TOKEN}` (env-expanded, no literal token committed); the old localhost entries survive as `*-local`. The prod WIF/Environment + `prod-rollout.yml` (drafted earlier) are live and were exercised by the successful prod dispatch (gate ✓, approval gate paused & approved, copy-by-digest, deploy). Test-project CI/CD wired separately: `scripts/setup_test_wif.sh` + `deploy.yml` triggers restored behind a `preflight` guard (P9b, see P10 row). **Original draft notes:** **`.github/workflows/prod-rollout.yml` NEW + committed (code only — ahead of the buildout).** `workflow_dispatch` (input `image_tag`, default `latest`) + `release: published`; **never on push**. `gate` job mirrors `deploy.yml` (postgres + tests + `ci_wrapper_smoke` + `check_openapi_snapshot`). `promote-and-deploy` job: `environment: prod` (manual-approval gate), separate prod WIF (`GCP_PROD_WIF_PROVIDER`/`GCP_PROD_DEPLOY_SA`); resolves each of the 3 images' **digest** in the test AR (`gcloud artifacts docker images describe … --format='value(image_summary.digest)'`), **B2 copy-by-digest** test AR → prod AR, then `gcloud run deploy`/`jobs update` against prod **by digest, image-only** (per-service config preserved from the manual P5–P7 first deploy). **PROMOTE STEP REWRITTEN 2026-06-17** (the P3 follow-up): copy now uses `gcloud auth configure-docker` + **`docker buildx imagetools create --tag <prod>:$TAG <test>@<digest>`** (not the non-existent `gcloud artifacts docker images copy`); since `imagetools` wraps the prod tag in a new OCI **index**, the workflow re-`describe`s `<prod>@<original-digest>` to confirm the original manifest landed byte-identically and **deploys by that original digest, not the tag**. YAML re-validated. Header documents the one-time prod-WIF setup + deploy-SA roles (`run.developer`, `artifactregistry.writer` on prod AR, `iam.serviceAccountUser` on prod runtime SA, `artifactregistry.reader` on the **test** AR). YAML validated. **Still open (can't be DONE until prod exists):** the `prod` GitHub Environment + reviewers, the prod WIF provider/SA + repo secrets, and the first manual dispatch. **Clients:** add a prod entry pointing at the prod wrapper `/mcp` URLs with prod-`QUANTCORE_MCP_TOKEN`; test URLs become staging. **DISPATCH PREREQ discovered 2026-06-17:** `workflow_dispatch` only exposes a workflow once it lives on the **default branch (`main`)** — but `.github/workflows/*` exist only on `feature/new-architecture-phase1` (main is the pre-Phase-1 state, 42 commits behind). So `prod-rollout.yml` is **not dispatchable until the feature branch is merged to `main`** (clean fast-forward; merge via PR so the first `main` push is deliberate). **`deploy.yml` NEUTRALIZED 2026-06-17 ("neutralize now, wire later"):** the test-project WIF + `GCP_WIF_PROVIDER`/`GCP_DEPLOY_SA` secrets were **never created** (verified: no WIF pool / no deploy SA in `quantcore-test-20260606`), so `deploy.yml`'s `deploy` job would fail auth on the merge's `main` push. To keep the merge green, `deploy.yml` is now **`workflow_dispatch`-only** (push/PR triggers commented out); image builds stay manual (`gcloud builds submit`) as in P7–P9. **FOLLOW-UP:** create the test WIF pool/provider + `quantcore-deployer@quantcore-test` SA (`run.developer`, `cloudbuild.builds.editor`, `artifactregistry.writer`, `iam.serviceAccountUser` on the runtime SA), set the 2 repo secrets, then restore `deploy.yml`'s `push: branches:[main]` + `pull_request` triggers. **Promotion runbook:** [`docs/operations/prod-promotion.md`](../operations/prod-promotion.md) documents the full team procedure (merge-to-main prereq, tag selection, gate → approval → copy-by-digest → deploy, rollback). |
| P10 | Prod exit + docs | DONE | — | 2026-06-17 | Status header → COMPLETE (above). `CLAUDE.md` Phase 3 paragraph + `architectural-standard-v2.md` §11 updated to "prod is live." Promoted digests + prod service URLs recorded in the header. **Tail items landed this commit:** P9a — `.mcp.json` repointed at the prod wrapper `/mcp` URLs (`Authorization: Bearer ${QUANTCORE_MCP_TOKEN}` via env expansion, never a literal token); the 5 localhost entries kept as `*-local` for the docker-compose stack. P9b — new `scripts/setup_test_wif.sh` (idempotent: creates test WIF pool `github-test`/provider `github` repo-restricted, `quantcore-deployer@quantcore-test` SA with run.developer/cloudbuild.builds.editor/artifactregistry.writer + iam.serviceAccountUser on the runtime SA, prints the 2 repo secrets); `deploy.yml` push/PR triggers **restored** behind a `preflight` job that emits `has_creds` only when `GCP_WIF_PROVIDER`/`GCP_DEPLOY_SA` are present, so `deploy` skips gracefully until the script is run. P7 — `portfolio/yfinance_gateway.py` `get_latest_prices` hardened (see P7 row). Phase 3 fully closed. |

---

## Verification

- **Per GCP step:** the specific smoke listed in its row (health/JWT/listTools/job-execute), against `quantcore-prod-20260606` only.
- **DB safety:** confirm at every step that no command names the test project for *writes* and no command reseeds/migrates the prod DB; the report path is the only thing that touches prod data and it does so exactly as it does today locally.
- **Artifact integrity (B2):** the prod-AR image digest equals the test-AR digest it was copied from (copy-by-digest guarantees this — spot-check with `gcloud artifacts docker images describe`).
- **Exit:** prod `quantcore-api` + 5 wrappers + report Job all healthy on Cloud Run in the prod project, JWT-enforced, pulling images from the **prod** AR (zero cross-project pull at runtime), pointed at the prod Cloud SQL.

## Risks & mitigations

- **Accidental prod-DB write/reseed** → no `import_portfolio.py`/migration against prod; read-only smokes; P0 locks "no reseed."
- **Rebuild divergence** → eliminated by B2 (copy by digest, never rebuild for prod).
- **Cross-project coupling creeping back** → prod pulls only from the prod AR at runtime; the test AR is touched only by the one-time/promotion `copy`, never by a running prod service.
- **JWT key bleed across environments** → prod gets a freshly generated key in its own Secret Manager; the test key is never copied.
- **Auto-deploying prod on a routine merge** → prod workflow is `workflow_dispatch`/tag + a `prod` Environment gate; the Phase 3 `deploy.yml` continues to target only the test project.
- **Token issuance unresolved** → flagged as an open decision; wrappers are deployable but not human-usable until a real prod token-issuance story exists.

## Critical files / targets

- `docs/proposals/prod-rollout-plan.md` (this file) — the checkpoint log.
- `.github/workflows/prod-rollout.yml` (new, Step P9) — gated prod promotion.
- `cloudbuild.yaml` — reused only as the build source for the test AR; prod consumes the **output** via digest copy, not a prod rebuild.
- GCP targets: prod project `quantcore-prod-20260606`, region `us-central1`, AR repo `quantcore` (new), Cloud SQL `quantcore-prod-20260606:us-central1:quantcore` (existing, live data).
- Reference: [`phase3-gateway-plan.md`](phase3-gateway-plan.md) Steps 7–10 — the test-project equivalents this plan mirrors.
