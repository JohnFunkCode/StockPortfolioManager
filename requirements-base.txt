# requirements-base.txt — the lean dependency set.
#
# Everything EXCEPT the heavy ML stack (torch / transformers). This covers the
# FastAPI front door's non-FinBERT paths, the report job (main.py never scores
# sentiment), and all 5 MCP wrapper containers (thin HTTP clients; even
# options_analysis only imports the services layer, whose FinBERT load is lazy).
#
# The ML extras live in requirements-ml.txt; requirements.txt = base + ml.
pandas>=1.3.0
yfinance>=0.2.0
requests>=2.25.0
matplotlib>=3.10.3
python-dotenv>=1.1.1
boto3>=1.39.13
jinja2>=3.1.6
PyYAML>=6.0.3
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
fastmcp>=2.0.0
httpx>=0.27.0
feedparser>=6.0.0
psycopg2-binary>=2.9
