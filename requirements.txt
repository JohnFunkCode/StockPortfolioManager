# requirements.txt — full local/dev install (base + ML stack).
#
# Layered to keep container images lean (see requirements-base.txt /
# requirements-ml.txt). `pip install -r requirements.txt` installs everything,
# exactly as before, so the local dev workflow is unchanged.
-r requirements-ml.txt
